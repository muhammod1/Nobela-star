import React from "react";

const BlueButton = ({text}) => {
  return (
    <button className="bg-[#417ac0] text-[white] py-2 px-4 rounded-lg">
      {text}
    </button>
  );
};

export default BlueButton;
